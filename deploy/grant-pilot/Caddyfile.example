{$SOVEREIGNKIT_COLLECTOR_HOSTNAME} {
	encode zstd gzip
	header {
		-Server
		Cache-Control "no-store"
	}

	@ingest {
		method POST
		path /v0/probe-results
	}

	handle @ingest {
		request_body {
			max_size 256KB
		}
		reverse_proxy 127.0.0.1:8787
	}

	handle {
		respond "not found" 404
	}
}
