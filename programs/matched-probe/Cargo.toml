[package]
name = "sovereignkit-matched-probe"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
solana-account-info = "=3.1.0"
solana-msg = "=3.0.0"
solana-program-entrypoint = "=3.1.1"
solana-program-error = "=3.0.0"
solana-pubkey = "=4.1.0"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
